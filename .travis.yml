language: java
jdk:
  - oraclejdk8
before_script:
  - chmod +x gradlew
script:
  - ./gradlew check
  - ./gradlew codeCoverageReport
dist: trusty
sudo: false
after_success:
  - bash <(curl -s https://codecov.io/bash)
