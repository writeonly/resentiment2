apply plugin: "jacoco"

dependencies {
    compile project (":ipu:corn")
}


jacocoTestReport {
    reports {
        xml.enabled false
        csv.enabled false
        html.destination file("${buildDir}/jacocoHtml")
    }
}